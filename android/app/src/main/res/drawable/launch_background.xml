<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash background (pre-Android-12). Solid vault green so the native
     window matches the Flutter splash — no white flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/vaultie_splash" />
</layer-list>
